name: NodeJS with Grunt

on:
  push:
    branches: [ "main", "master", "dev" ]
  pull_request: 
    branches: [ "main", "master", "dev" ]  

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: "Build"
      run: "npm run buld"
    - name: "comment"
      run: echo "build exitoso"  
