{
  "version": 2,
  "builds": [
    {
      "src": "server/server.index",
      "use": "@vercel/node"
    }
  ],

  "routes": [
    {
      "src": "/(.*)",
      "dest": "server/api/vercel.ts"
    }
  ]
}
