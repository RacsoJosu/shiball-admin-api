{
  "version": 2,
  "builds": [
    {
      "src": "server/api/vercel.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server/api/vercel.ts"
    }
  ],
   "buildCommand": "npm run build && npx prisma generate"

}
